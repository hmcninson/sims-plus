import { StaffAttendanceMarking } from "@/components/attendance/staff-attendance-marking";

export const metadata = {
  title: "Mark Staff Attendance",
};

export default function MarkStaffAttendancePage() {
  return <StaffAttendanceMarking />;
}
