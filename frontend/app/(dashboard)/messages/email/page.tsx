import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = {
  title: "Email",
};

export default function EmailPage() {
  return <PlaceholderPage title="Email" description="Send email communications." />;
}
