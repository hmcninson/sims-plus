import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = {
  title: "Examinations",
};

export default function ExamsPage() {
  return <PlaceholderPage title="Examinations" description="Manage exams and assessments." />;
}
