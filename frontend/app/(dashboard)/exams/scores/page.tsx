import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = {
  title: "Score Entry",
};

export default function ScoreEntryPage() {
  return <PlaceholderPage title="Score Entry" description="Enter and manage exam scores." />;
}
