import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = {
  title: "Routes",
};

export default function RoutesPage() {
  return <PlaceholderPage title="Routes" description="Manage transport routes and stops." />;
}
