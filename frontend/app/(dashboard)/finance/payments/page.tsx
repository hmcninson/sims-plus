import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = {
  title: "Payments",
};

export default function PaymentsPage() {
  return <PlaceholderPage title="Payments" description="Track and record payments." />;
}
